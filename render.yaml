services:
- type: web
  name: smartparking-api
  env: java
  buildCommand: mvn clean package -DskipTests
  startCommand: java -jar target/smartparking-*.jar
  envVars:
    - key: PORT
      sync: false
